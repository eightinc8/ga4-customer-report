# 本番環境用 - VPSの /var/www/ga4-customer-report/.env にコピー
# 各値を必ず変更してください

NODE_ENV=production

# JWT署名用シークレット（32文字以上のランダム文字列に変更）
JWT_SECRET=ここを32文字以上のランダム文字列に変更してください

# GASからのデータPush用APIキー（ランダム文字列に変更）
GAS_API_KEY=ここをランダムなAPIキーに変更してください
